// import path from 'path';

// const pdfToImages = async (filePath, pageIndexes) => {
//   const outputDir = path.dirname(filePath);
//   const baseName = path.basename(filePath, path.extname(filePath));
//   const converter = fro;
// };